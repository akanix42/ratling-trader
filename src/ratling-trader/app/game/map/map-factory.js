define(function (require) {
    var Map = require('game/map/map');

    return Constructor;

    function Constructor() {
        var self = this;

        var map = new Map();

        return map;
    }
});